package nl.streats1.cobbledollarsvillagersoverhaul.integration;

/** Item ID + CobbleDollars value per 1 item. For custom currency config UI. */
public record CurrencyEntryRecord(String itemId, int value) {}
